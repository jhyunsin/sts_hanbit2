<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.account">
 
 
 <update id="insertAccount" parameterType="com.hanbit.web.bank.AccountVO">
 insert into account (id, account_no, money)values(#{id},#{accountNo},#{money})</update>
 <select id="getRestMoney" parameterType="int" resultType="int">
 select money from account where account_no = #{accountNo}</select>
 <select id="list"  resultType="com.hanbit.web.bank.AccountVO">
 select account_no as acc, id as id, money as money, name as name, ssn as birth from account_member order by name</select>
 
 <select id="selectMoney" parameterType="int" resultType="int">
 select money from account where account_no=#{accountNo}</select>
 <select id="count" resultType="int">
 select count(*) as count from account_member</select>


<update id="deposit" parameterType="com.hanbit.web.bank.AccountVO">
update account set money=? where account_no = #{accountNo}</update>
<update id="withdraw" parameterType="com.hanbit.web.bank.AccountVO">
update member set pw= #{pw} , email = #{email} where id = #{id}</update>

<delete id="deleteAccount" parameterType="int">
delete from account where account_no=#{accountNo}</delete>
</mapper>


